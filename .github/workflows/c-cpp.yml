name: C/C++ CI

on:
  push:
    branches: [ "main", "divider" ]
  pull_request:
    branches: [ "main", "divider" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: make check
      run: make check
